export const CALENDLY_DISCOVERY_SESSION_URL =
  'https://calendly.com/gade/schedule_your_free_consultation';

export const CALENDLY_PROFILE_URL = 'https://calendly.com/gade';

/** Dedicated Up4Growth page with the embedded Calendly scheduler. */
export const BOOK_PAGE_PATH = '/book';

/** Dedicated Up4Growth page with the embedded Cal.com scheduler (home Get in Touch). */
export const GET_IN_TOUCH_PAGE_PATH = '/get-in-touch';

/** Cal.com event path used by the Get in Touch embed. */
export const HOME_GET_IN_TOUCH_CAL_LINK = 'chandini-majji-tq29od/30min';

/** Full Cal.com URL (used to build the embed iframe src). */
export const HOME_GET_IN_TOUCH_URL = `https://cal.com/${HOME_GET_IN_TOUCH_CAL_LINK}`;
